# Runtime Evidence — 2026-09-04

Version `ba2bdf88f3dbe650bb98158628cec708534eaacb`, branch `main`, local Next.js dev server port 3000. Database identity remains `NEEDS CONFIRMATION`; no create/delete/live-AI success path was executed.

| Check | Observed result |
|---|---|
| `GET /` | 200, HTML |
| `GET /katalog` | 200, HTML |
| `GET /api/catalog` | 200; success; 15 rows; all published |
| `GET /api/products` without cookie | 200; success; 16 rows; parked and published |
| `GET /admin` without cookie | 307 → `/login?from=%2Fadmin` |
| `GET /admin` with random `sc_session` | 200 |
| Invalid credential login | 401; generic error |
| Empty credential login | 400; required-field error |
| Malformed JSON login | 500; empty body; server SyntaxError |
| Logout without cookie | 200; success; cookie deletion header |
| `GET /api/admin/review` without cookie | 200; 4 rows |
| Products empty array | 400; minimum-one-item error |
| Products empty item | 500; internal TypeError message exposed |
| Product invalid status | 400; enum error |
| Product nonexistent ID publish | 404 |
| Product delete missing ID | 400 |
| Ingest empty array | 400; minimum-one-item error |
| Ingest 31 items | 400; maximum-30 error; no AI call |
| Review PATCH missing ID | 400 |
| Upload without files | 400 |
| Unknown product slug | 200 HTML; expected behavior unresolved |

The in-app browser runtime reported no available browser (`[]`). The repository Playwright package was available, its downloaded Chromium binary was absent, and installed Microsoft Edge at `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe` launched successfully in headless mode. UI execution therefore continued through Playwright + Edge instead of being marked globally blocked.

## Browser execution evidence

- Browser: Microsoft Edge headless via Playwright 1.62.1; application `http://127.0.0.1:3000`.
- Public trace: `docs/qa/evidence/ui-2026-09-04/public-flow-trace.zip`.
- Auth-negative trace: `docs/qa/evidence/ui-2026-09-04/auth-negative-trace.zip`.
- Admin trace: `docs/qa/evidence/ui-2026-09-04/admin-flow-trace.zip`.
- Screenshots: empty-cart repopulation, forged-cookie admin access, review-index failure, invalid slug fallback, non-image upload, catalog API fallback, and public mobile menu under `docs/qa/evidence/ui-2026-09-04/`.
- External Unsplash/placehold.co image requests were denied by the execution environment (`net::ERR_NETWORK_ACCESS_DENIED`). This is an environment limitation/dependency risk, not classified as an application defect.
- Full UI actual results, roles, flows, API correlation, IDs, and evidence paths are in `docs/qa/ui-exploratory-execution.tsv`.

Only the valid-credential login/logout lifecycle, destructive DB transitions, live Gemini success path, and one isolated mobile-admin drawer check remain blocked. Their blockers are specific credentials/environment/safe-data constraints, not generic browser unavailability.

The server log also emitted a Next.js 16 deprecation warning for the `middleware.ts` convention and recommended `proxy`; recorded as maintenance risk only.
